# Framer company page reference

Extracted from git `HEAD:public/company/` for visual/copy comparison only.
Not served by Next.js — do not add rewrites to these files.

- `index.html` — `/company` (PR)
- `hr.html` — `/company/hr`
